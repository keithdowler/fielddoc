# FieldDoc Mobile

Expo Router mobile placeholder for Sprint 0. This app is configured for development builds through `expo-dev-client`.

## Commands

Run from the repository root:

```bash
pnpm dev:mobile
pnpm --filter @fielddoc/mobile ios
pnpm --filter @fielddoc/mobile android
```
